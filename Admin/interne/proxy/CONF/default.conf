server {
    listen 80;
    server_name logs.lan.interne;

    location / {
        resolver 127.0.0.11 valid=30s;
        set $upstream_kibana kibana;
        proxy_pass http://kibana:5601; # Redirige vers Kibana
    }
}